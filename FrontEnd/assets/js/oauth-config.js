/* ============================================
   oauth-config.js – Cấu hình đăng nhập Google/Facebook
   Điền Client ID/App ID thật của bạn vào đây.
   (Đây là ID công khai, an toàn khi để ở frontend —
    KHÔNG bao giờ đặt Client Secret / App Secret ở đây)
   ============================================ */

// Lấy tại https://console.cloud.google.com/apis/credentials
// (OAuth client ID → Application type: Web application)
const GOOGLE_CLIENT_ID =
  "828745700187-0ggnndhdogv429u6tkq451142l338tsd.apps.googleusercontent.com";

// Lấy tại https://developers.facebook.com/apps (App ID hiển thị ở Dashboard)
const FACEBOOK_APP_ID = "2354715051602679";
